import { ExtractTagsPipe } from './extract-tags.pipe';

describe('ExtractTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
